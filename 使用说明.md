# 随手工具箱

电脑打开 http://localhost:8765/ ，手机连接同一 Wi-Fi 后，用电脑的局域网 IP 替换 `localhost`。
首页点击「随它转」进入转盘，也可直接访问 `/tools/decision-wheel/`。

每行输入一个选项，点击「生成转盘」，然后「开始转动」。
支持 2–24 项、每项最多 40 字。每个格子概率相同，重复选项会占多个格子。
输入只暂存在当前浏览器，刷新后仍保留；其他设备不共享。

电脑须保持开机、不休眠。重启后，双击「启动工具箱.command」并保留终端窗口；原来的「启动转盘.command」同样可用。
若提示端口已占用，先尝试直接访问网站。若电脑的局域网 IP 变化，需要更新手机访问地址。
详细开发和构建方法见 [README](README.md)。
